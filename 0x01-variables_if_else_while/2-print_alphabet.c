#include <stdio.h>

int main(void)
{

/* lower case alphabet */

/* al is for lower case alphabet */

	char al;

	for (al = 'a'; al <= 'z'; al++)
	{
		putchar(al);
	}
		putchar('\n');

	return (0);
}
