#include <stdio.h>
#include <stdlib.h>
/**
 * main - header
 *
 * Return: return always(0) succes
 */

int main(void)
{
	char al;

	for (al = 'a'; al <= 'z'; al++)
	{
		putchar(al);
	}
	putchar('\n');

	return (0);
}
