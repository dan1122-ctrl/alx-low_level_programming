#include <stdio.h>

int main(void)
{

/**
 * main - header
 *
 * Return: return always(0) succes

/* al is for lower case alphabet */

	char al;

	for (al = 'a'; al <= 'z'; al++)
	{
		putchar(al);
	}
		putchar('\n');

	return (0);
}
