#include "main.h"
/**
 * add - adds two integers and returns the result
 * @i: is the first intiger
 * @j: is the second intiger
 * Return: sum of both intigers
 */
int add(int i, int j)
{
	int i, j;

	return (i + j);
}
