#include "main.h"
/**
 * add - adds two integers and returns the result
 * @a: is the first intiger
 * @b: is the second intiger
 * Return: sum of both intigers
 */
int add(int a, int b)
{

	return (a + b);
}
