#include "main.h"
/**
 * print_alphabet - checkout the code
 *
 * Reprint: turn: always 0
 */
void print_alphabet(void)
{
	char al = 'a';

	for (al = 'a'; al <= 'z'; al++)
		_putchar(al);
	_putchar('\n');
	return (0);

}
