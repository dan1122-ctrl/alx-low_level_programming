#include <stdio.h>
#include "main.h"
/**
 * mul - multiplies two integers
 * @a:is first inginer
 * @b:is second intiger
 * Return: multiplication of two intigers
 */

int mul(int a, int b)
{
	return = (a * b);
}
